# ============================================================
# madfinder 환경변수 목록 (server + batch 공용)
# 사용법: 이 파일을 복사해 .env 로 만들고 값을 채운다 (.env는 git 제외)
#         또는 OS 환경변수로 직접 등록
# ============================================================

# MySQL 접속 호스트. 로컬 개발은 비워둬도 됨 (기본값 localhost)
# Docker 배포 시 compose가 mysql 컨테이너 이름을 주입
DB_HOST=

# MySQL root 비밀번호 (각자 자기 것)
DB_PASSWORD=

# (선택) F-10 유튜브 영상 기능 사용 시
YOUTUBE_API_KEY=
