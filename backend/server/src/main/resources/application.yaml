spring:
  application:
    name: server
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/roblox_rec
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
