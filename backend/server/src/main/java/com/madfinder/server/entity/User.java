package com.madfinder.server.entity;

/**
 * users 테이블 매핑. (원본: docs/KJH/db-schema.sql)
 * 컬럼: user_id(PK, 로블록스 userId), username, display_name, last_seen_at
 * 서비스 이용자. 로그인 없음 — 닉네임 재입력으로 식별
 * TODO(KJH): @Entity/@Table/@Id 및 필드 작성. 복합 PK는 @IdClass 사용.
 */
public class User {
}
