package com.madfinder.server.repository;

/**
 * chart_snapshot 접근 계층. (담당: KJH — 쿼리 전부 여기에)
 * 주요: 전체 덮어쓰기 UPSERT(B1), 캐싱 대상 선정 조회
 * TODO(KJH): JpaRepository 상속으로 전환 후 쿼리 메서드/@Query 작성.
 */
public interface ChartSnapshotRepository {
}
