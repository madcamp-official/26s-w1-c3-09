package com.madfinder.server.service;

/**
 * 게임 정보 캐시 우선 조회 로직. (담당: BMS)
 * "games에 있으면 DB에서, 없거나 updated_at 낡았으면 로블록스 B-1 호출 후 저장" 패턴 공통화.
 * E5 표시용 일괄 조회, E7 상세에서 재사용.
 * TODO(BMS): 구현.
 */
public class GameService {
}
