package com.madfinder.server.controller;

/**
 * E3. GET /api/search?q={검색어} — 게임 이름 검색 (티어표 직접 추가용). (담당: BMS)
 * 로블록스 A-3(omni-search) 호출 → 상위 10개 {universeId, name, playerCount, iconUrl}.
 * DB 사용 안 함 (항상 실시간 — 로블록스가 최신 이름을 앎).
 * TODO(BMS): 구현.
 */
public class SearchController {
}
